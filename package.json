{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "cd artifacts/api-server && $env:PORT=5000; pnpm run build && pnpm run start",
    "dev": "concurrently \"pnpm run dev:frontend\" \"pnpm run dev:video\" \"pnpm run dev:api\"",
    "dev:frontend": "cd artifacts/cyber-safety-hub && $env:PORT=3000; $env:BASE_PATH='/'; pnpm run dev",
    "dev:video": "cd artifacts/cyber-surakshit-video && $env:PORT=3001; $env:BASE_PATH='/'; pnpm run dev",
    "dev:api": "cd artifacts/api-server && $env:PORT=5000; pnpm run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
